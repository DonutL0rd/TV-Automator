# TV-Automator Environment Variables
# Copy to .env and fill in values

# Display configuration (auto-detected in Docker, set manually for dev)
# DISPLAY=:0

# Chrome path override (auto-detected by default)
# CHROME_PATH=/usr/bin/google-chrome

# Data directory for persistent state (cookies, config)
# DATA_DIR=/data
